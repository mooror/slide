var -extra name = function(config) {
    config = config || {};
    -extra name.superclass.constructor.call(this,config);
};
Ext.extend(-extra name,Ext.Component,{
    page:{},window:{},grid:{},tree:{},panel:{},combo:{},config: {}
});
Ext.reg('-extra name',-extra name);

-extra name = new -extra name();